from setuptools import setup

setup(
      name='Capio_Parse_Transcript',
      version='0.1',
      description='Capio Parse Transcript',
      packages=['capio'],
      install_requires=['docx', 'requests']
)
